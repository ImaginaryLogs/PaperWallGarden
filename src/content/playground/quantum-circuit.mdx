---
title: "Interactive Quantum Circuit Simulator"
description: "Apply quantum gates and visualize qubit state on the Bloch sphere"
tags: ["quantum", "wasm", "interactive"]
---

import QuantumPlaygroundContainer from '../../components/playground/QuantumPlaygroundContainer';
import CrossLink from '../../components/shared/CrossLink.astro';

# Quantum Circuit Simulator

This playground lets you build quantum circuits gate-by-gate and observe
the resulting qubit state live on the Bloch sphere.

<CrossLink to="garden:quantum/bloch-sphere-notes" label="View Bloch Sphere Mathematical Foundations" />

## Native Engine Execution

Below, we instantiate our compiled assembly module. When the script downloads, the underlying WASM runtime safely exposes its bindings directly to the UI.

{/* ✅ This clean, single string property safely crosses the Astro-to-React boundary without serialization issues! */}
<QuantumPlaygroundContainer client:only="react" modulePath="/wasm/quantum-sim/quantum_sim.js" />

## Mathematical Context

The system renders equations using LaTeX via our rehype configurations:

$$
|\psi\rangle = \alpha|0\rangle + \beta|1\rangle
$$

The transition mappings execute linearly across near-zero memory footprint barriers.