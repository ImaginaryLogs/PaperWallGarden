[package]
name = "petri-sim"
version = "0.1.0"
edition = "2021"
description = "Petri Net Business Process Simulator compiled to WebAssembly"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"

[profile.release]
# Minimise binary size for fast page loads
opt-level = "s"
lto = true