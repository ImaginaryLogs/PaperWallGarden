**Network-graph CA** generalize the grid by replacing the regular lattice with an arbitrary graph. Now each node is still an FSA, but its "neighborhood" is its graph neighbors. This lets you model irregular topologies — social networks, neural wiring, road networks — where spatial regularity doesn't exist. The automaton logic is the same; the connectivity structure is what changes.