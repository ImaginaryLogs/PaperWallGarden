**Feature maps** (including the ZZ feature map) come from QML. They're circuits that encode classical data (a vector x) into a quantum state. The ZZ feature map specifically encodes pairwise products of features via ZZ-type entangling interactions, which is what gives it expressiveness beyond a linear kernel. You can think of it as: "here is a nonlinear embedding of my classical data into Hilbert space."