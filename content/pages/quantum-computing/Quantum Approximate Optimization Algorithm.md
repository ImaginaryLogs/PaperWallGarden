**Phase estimation (right column)** is a fault-tolerant algorithm. QPE can extract the eigenvalue (and thus the energy) of a Hamiltonian to arbitrary precision, but it requires long coherent circuits with error correction. It's the "right" answer to what VQE approximates, but currently unreachable without fault-tolerant hardware. For quantum chemistry, QPE with Trotterization is the long-term target.