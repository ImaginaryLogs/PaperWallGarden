These are circuits where some gate rotation angles (like the θ in Rx(θ)) are left as free variables. 

Instead of a fixed circuit, you have a family of circuits. This is the foundation of everything variational.